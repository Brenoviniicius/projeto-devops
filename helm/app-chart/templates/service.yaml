apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-devops-pratica.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "app-devops-pratica.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}